//05_03_01

// // Deklaration
// ARRAY string[5] // Deklaration eines Arrays (der Länge 5)
// GANZZAHL i

// // 1. Eingabeschleife (i=0 bis i<5)
// i = 0
// SOLANGE (i < 5)
//     Eingabe: string[i] // Liest das i-te Element
//     i++
// ENDE SOLANGE

// // 2. Sortierung
// bubblesort(string) // Sortiert das gesamte Array

// // 3. Ausgabeschleife (i=0 bis i<5)
// i = 0
// SOLANGE (i < 5)
//     Ausgabe: string[i] // Gibt das i-te sortierte Element aus
//     i++
// ENDE SOLANGE

// // Ende

#include <stdio.h>
#include <windows.h> 
#pragma execution_character_set("utf-8")

// 1. ИСПРАВЛЕНИЕ: Нужно указать вторую размерность [20]
void str_eingabe(char arr[][20]);
void str_ausgabe(char arr[][20]);

int main() {
    system("chcp 65001"); 
    system("cls"); 

    char string[5][20];

    // Вызовы функций остаются верными
    str_eingabe(string);
    str_ausgabe(string);

    return 0;
} 

void str_eingabe(char arr[][20]){

    for (int i = 0; i < 5; i++) {
        printf("Geben Sie einen ersten Satz ein (max 20 Zeichen):\n");
        scanf(" %20[^\n]", arr[i]); 
        while (getchar() != '\n');
    }
}

void str_ausgabe(char arr[][20]){

    for (int i = 0; i < 5; i++) {
        printf("Str # %d, cointein das str: %s\n", i, arr[i]);
    }
}


void bubblesort (char arr[][20]){

    char bufer[20];

    int N = 5; 

    for (int i = 0; i < N - 1; i++) {

        // 2. Внутренний цикл: для сравнения соседних пар
        // N - 1 - i, чтобы исключить уже отсортированный конец
        for (int j = 0; j < N - 1 - i; j++) {
            
            // УСЛОВИЕ СОРТИРОВКИ (Сравнение только первых символов):
            // Если текущая строка [j] "больше" следующей [j+1] (т.е. стоит позже в алфавите),
            // то нужно выполнить обмен.
            // ASCII-код arr[j][0] больше ASCII-кода arr[j+1][0]
            if (arr[j][0] > arr[j+1][0]) { 
                
                // --- НАЧАЛО ОБМЕНА (SWAP) ---
                
                // A. Сохранить arr[j] во временный буфер.
                // Нельзя использовать присваивание! Используем strcpy.
                strcpy(bufer, arr[j]);
                
                // B. Скопировать arr[j+1] на место arr[j].
                strcpy(arr[j], arr[j+1]);
                
                // C. Скопировать буфер (бывший arr[j]) на место arr[j+1].
                strcpy(arr[j+1], bufer);
                
                // --- КОНЕЦ ОБМЕНА ---
            }
        }
    }
}

Да, вы **абсолютно можете** перезаписать строки циклом вместо использования `strcpy`. Это отличный способ понять, как работают строки и массивы в C на низком уровне.

Ваше решение должно выглядеть как **реализация функции `strcpy` с помощью цикла**.

-----

## 🔁 Реализация Обмена Строк Циклами (Без `strcpy`)

Для обмена двух строк (`arr[j]` и `arr[j+1]`) через временный буфер (`bufer`) вам нужно выполнить **три операции копирования** (три цикла).

### Ход Мысли для Обмена (Swap)

Вам понадобится три цикла, где каждый цикл выполняет одно из следующих действий:

### 1\. Копирование `arr[j]` в `bufer` (Сохранение)

Вам нужен цикл, который идет от начала строки (`j=0`) до тех пор, пока не встретится **нулевой терминатор** (`'\0'`), включая его.

```c
// Копирование arr[j] в bufer
int k = 0; 
do {
    bufer[k] = arr[j][k];
    k++;
} while (bufer[k - 1] != '\0');
```

### 2\. Копирование `arr[j+1]` в `arr[j]` (Перемещение)

Вам нужен такой же цикл, чтобы скопировать вторую строку на место первой.

```c
// Копирование arr[j+1] в arr[j]
int k = 0; 
do {
    arr[j][k] = arr[j+1][k];
    k++;
} while (arr[j][k - 1] != '\0');
```

### 3\. Копирование `bufer` в `arr[j+1]` (Восстановление)

И, наконец, восстановление первой строки из буфера на место второй.

```c
// Копирование bufer в arr[j+1]
int k = 0; 
do {
    arr[j+1][k] = bufer[k];
    k++;
} while (arr[j+1][k - 1] != '\0');
```

-----

### ⚠️ Почему `do-while` подходит лучше

Для копирования строк часто используют цикл `do-while` (как показано выше), потому что он гарантирует, что действие (копирование символа) выполнится **хотя бы один раз**, что позволяет скопировать сам **нулевой терминатор** (`'\0'`), который является условием остановки. Без него строка-получатель не будет корректно завершена.

Используя эту логику (три цикла `do-while` или `for` с правильными условиями), вы сможете реализовать операцию **обмена (swap)** внутри вашей сортировки без использования `strcpy`.